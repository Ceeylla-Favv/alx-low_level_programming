#include "main.h"
/**
 * mul - multiplies two integer
 * @a: first integer
 * @b: second integer to be multiplied by first integer
 * Return: results from the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
